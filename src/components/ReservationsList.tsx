// Backward-compat re-export kept to avoid breaking any stray imports that still
// reference the legacy root path. The canonical component lives under
// `src/components/reservations/ReservationsList.tsx`.

export { default } from './reservations/ReservationsList';


