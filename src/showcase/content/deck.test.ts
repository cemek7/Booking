import { describe, expect, it } from '@jest/globals'; import { DECK } from './deck'; describe('DECK', () => { it('has 35–50 specific slides including the disclosure', () => { expect(DECK.length).toBeGreaterThanOrEqual(35); expect(DECK.length).toBeLessThanOrEqual(50); expect(DECK.every(s => s.title.trim() && s.body.trim())).toBe(true); expect(DECK.some(s => s.title === 'Disclosure')).toBe(true); }); });
