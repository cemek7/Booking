import { describe, expect, it } from '@jest/globals'; import { SUNGRID_CASE_STUDY } from '@/showcase/content/case-studies/sungrid-energy'; import { renderUpwork } from './upwork'; import { renderLinkedIn } from './linkedin'; import { renderProposal } from './proposal';
describe('case-study renderers', () => { it('renders an honest 300–600 word Upwork export', () => { const output=renderUpwork(SUNGRID_CASE_STUDY); const words=output.trim().split(/\s+/).length; expect(words).toBeGreaterThanOrEqual(300); expect(words).toBeLessThanOrEqual(600); expect(output).toContain(SUNGRID_CASE_STUDY.disclosure); expect(output).not.toContain('measuredScores'); }); it('renders LinkedIn hook, problem, decisions, result, and disclosure', () => { const output=renderLinkedIn(SUNGRID_CASE_STUDY); expect(output).toContain('The problem:'); expect(output).toContain('Three decisions:'); expect(output).toContain('What was delivered:'); expect(output).toContain(SUNGRID_CASE_STUDY.disclosure); }); it('renders all five proposal sections', () => { const output=renderProposal(SUNGRID_CASE_STUDY); expect(Object.keys(output).sort()).toEqual(['approach','cta','problem','proofPoints','relevantDemo']); }); });
