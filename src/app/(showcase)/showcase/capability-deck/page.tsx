import { DECK } from '@/showcase/content/deck'; export const metadata = { title: 'Capability deck', description: 'TechClave capability deck.' }; export default function CapabilityDeckPage() { return <main className="bg-slate-950 p-6 text-slate-50 sm:p-12">{DECK.map((slide,i)=><section key={slide.title} className="mx-auto mb-8 flex aspect-video max-w-6xl flex-col justify-between border border-white/20 p-8 sm:p-16"><p className="text-sm text-emerald-300">{String(i+1).padStart(2,'0')} / {DECK.length}</p><div><p className="text-xs uppercase tracking-[.2em] text-slate-400">TechClave</p><h1 className="mt-5 max-w-4xl text-4xl font-semibold sm:text-6xl">{slide.title}</h1><p className="mt-6 max-w-2xl text-lg text-slate-300">{slide.body}</p></div><p className="text-sm text-slate-400">Capability system · truthful by design</p></section>)}</main>; }
