import { LeadForm } from '@/components/capability/forms/LeadForm'; export const metadata={title:'Contact — TechClave',description:'Start a local demonstration project inquiry.'}; const fields=[{name:'name',label:'Name',required:true},{name:'email',label:'Email',type:'email' as const,required:true},{name:'company',label:'Company'},{name:'website',label:'Website'},{name:'service',label:'Service needed',required:true},{name:'budget',label:'Budget range'},{name:'timeline',label:'Timeline'},{name:'summary',label:'Project summary',type:'textarea' as const,required:true}]; export default function Contact(){return <main className="mx-auto max-w-3xl px-6 py-20"><h1 className="text-5xl font-semibold">Start with the problem.</h1><p className="mt-4 text-lg text-slate-600">This local demonstrator form sends and stores nothing.</p><div className="mt-10 rounded-2xl border p-7"><LeadForm fields={fields} submitLabel="Send project inquiry" /></div></main>}
