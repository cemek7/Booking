import { Card } from '@/components/capability/core/Card';
import { Container } from '@/components/capability/core/Container';
import { Heading } from '@/components/capability/core/Heading';
import { Section } from '@/components/capability/core/Section';
import { Text } from '@/components/capability/core/Text';
const people = [['Amina O. Dike', 'Corporate & commercial', 'Illustrative profile focused on commercial decisions and governance conversations.'], ['Chidi Eze', 'Transactions', 'Illustrative profile for transaction planning and cross-functional coordination.'], ['Nneka Afolabi', 'Dispute resolution', 'Illustrative profile for resolution-process information and first-contact clarity.']];
export const metadata = { title: 'Team — Meridian Legal', description: 'Illustrative attorney profiles for the Meridian Legal capability demonstrator.' };
export default function TeamPage() { return <main><Section><Container width="wide"><p className="text-xs font-semibold uppercase tracking-[.18em]" style={{ color: 'var(--sc-eyebrow)' }}>People</p><Heading level={1} className="mt-3">A profile system designed for context, not credentials theatre.</Heading><Text size="lg" tone="muted" className="mt-4 max-w-2xl">These names and biographies are fictional. A production firm site can use this structure to help prospective clients understand relevant experience without implying a result.</Text><div className="mt-10 grid gap-px border border-current/10 bg-current/10 md:grid-cols-3">{people.map(([name, role, copy]) => <Card key={name} className="rounded-none border-0"><div className="flex size-12 items-center justify-center border border-current/20 text-sm font-semibold">{name.split(' ').map((word) => word[0]).join('')}</div><Heading level={2} className="mt-6 text-xl">{name}</Heading><p className="mt-2 text-sm font-medium" style={{ color: 'var(--sc-primary)' }}>{role}</p><Text tone="muted" className="mt-3">{copy}</Text></Card>)}</div></Container></Section></main>; }
