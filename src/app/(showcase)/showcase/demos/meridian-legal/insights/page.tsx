import { Card } from '@/components/capability/core/Card';
import { Container } from '@/components/capability/core/Container';
import { Heading } from '@/components/capability/core/Heading';
import { Section } from '@/components/capability/core/Section';
import { Text } from '@/components/capability/core/Text';
const insights = [['Board materials that make decisions easier to revisit', 'A sample editorial card showing the kind of clear, practical firm insight a content system can support.'], ['Preparing for a commercial negotiation', 'A general-information article pattern designed to explain a topic without offering legal advice.'], ['What a thoughtful first consultation can clarify', 'A content pattern for setting expectations around a first conversation and its boundaries.']];
export const metadata = { title: 'Insights — Meridian Legal', description: 'Illustrative legal insight cards for the Meridian Legal capability demonstrator.' };
export default function InsightsPage() { return <main><Section><Container width="wide"><p className="text-xs font-semibold uppercase tracking-[.18em]" style={{ color: 'var(--sc-primary)' }}>Insights</p><Heading level={1} className="mt-3">Useful reading, carefully bounded.</Heading><Text size="lg" tone="muted" className="mt-4 max-w-2xl">These illustrative article cards demonstrate an editorial system. They are not legal advice and do not describe a real firm’s publications.</Text><div className="mt-10 grid gap-6 md:grid-cols-3">{insights.map(([title, copy], index) => <Card key={title} className="flex min-h-64 flex-col justify-between rounded-none"><div><p className="text-xs uppercase tracking-[.16em] opacity-60">Insight · {String(index + 1).padStart(2, '0')}</p><Heading level={2} className="mt-5 text-2xl">{title}</Heading></div><Text tone="muted" className="mt-5">{copy}</Text></Card>)}</div></Container></Section></main>; }
