import { LeadForm } from '@/components/capability/forms/LeadForm'; import { Card } from '@/components/capability/core/Card'; import { Container } from '@/components/capability/core/Container'; import { Heading } from '@/components/capability/core/Heading'; import { Section } from '@/components/capability/core/Section'; import { Text } from '@/components/capability/core/Text';
const fields = [{ name: 'name', label: 'Parent or caregiver name', required: true }, { name: 'email', label: 'Email address', type: 'email' as const, required: true }, { name: 'childAge', label: 'Child age or entry year', required: true }, { name: 'question', label: 'Your question', type: 'textarea' as const, required: true }]; export const metadata = { title: 'Make an inquiry — Crestfield Academy', description: 'A local-only admissions inquiry in the Crestfield Academy capability demonstrator.' }; export default function Contact() { return <main><Section><Container width="wide" className="grid gap-10 lg:grid-cols-2"><div><Heading level={1}>Begin with a question.</Heading><Text size="lg" tone="muted" className="mt-4">This local-only form demonstrates an admissions inquiry. No data is sent, stored, or used to offer a place.</Text><Card className="mt-8"><Heading level={2}>Safeguarding</Heading><Text tone="muted" className="mt-3">A real school must publish verified safeguarding contacts and procedures. This fictional demonstrator provides no safeguarding service.</Text></Card></div><Card className="p-7"><Heading level={2}>Admissions inquiry</Heading><div className="mt-6"><LeadForm fields={fields} submitLabel="Send inquiry" /></div></Card></Container></Section></main>; }
