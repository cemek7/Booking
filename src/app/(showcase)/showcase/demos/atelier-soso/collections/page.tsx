import Link from 'next/link'; import { WishlistButton } from '@/components/capability/demo-shell/WishlistButton'; import { Card } from '@/components/capability/core/Card'; import { Container } from '@/components/capability/core/Container'; import { Heading } from '@/components/capability/core/Heading'; import { Section } from '@/components/capability/core/Section'; import { Text } from '@/components/capability/core/Text'; const items = [['The Fold Dress', '₦85,000'], ['Softline Set', '₦112,000'], ['Soso Carryall', '₦64,000']]; export const metadata = { title: 'Collections — Atelier Soso', description: 'Illustrative fashion collection cards.' }; export default function Collections() { return <main><Section><Container width="wide"><Heading level={1}>The current edit.</Heading><Text tone="muted" className="mt-4">Illustrative products and prices only. Saving a product happens in this browser session and creates no order.</Text><div className="mt-10 grid gap-6 md:grid-cols-3">{items.map(([name, price], i) => <Card key={name} className="rounded-none"><div className="aspect-[3/4]" style={{ background: i === 1 ? 'var(--sc-accent)' : 'color-mix(in srgb, var(--sc-primary) 8%, transparent)' }} /><Heading level={2} className="mt-5">{name}</Heading><Text tone="muted" className="mt-2">{price}</Text><div className="mt-5 flex gap-3"><WishlistButton item={name} /><Link className="text-sm underline" href="/showcase/demos/atelier-soso/contact">Inquire</Link></div></Card>)}</div></Container></Section></main>; }
