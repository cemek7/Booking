import { LeadForm } from '@/components/capability/forms/LeadForm'; import { Card } from '@/components/capability/core/Card'; import { Container } from '@/components/capability/core/Container'; import { Heading } from '@/components/capability/core/Heading'; import { Section } from '@/components/capability/core/Section'; import { Text } from '@/components/capability/core/Text'; const fields = [{ name: 'name', label: 'Your name', required: true }, { name: 'email', label: 'Email address', type: 'email' as const, required: true }, { name: 'product', label: 'Product or collection', required: true }, { name: 'message', label: 'Your inquiry', type: 'textarea' as const, required: true }]; export const metadata = { title: 'Product inquiry — Atelier Soso', description: 'A local-only product inquiry form.' }; export default function Contact() { return <main><Section><Container width="wide" className="grid gap-10 lg:grid-cols-2"><div><Heading level={1}>Ask about a piece.</Heading><Text size="lg" tone="muted" className="mt-4">This is a local demonstration form. No inquiry is sent, no stock is confirmed, and no purchase can be made here.</Text><Text tone="muted" className="mt-8">Shipping, returns, availability, and sizing must be confirmed by a real retailer. Atelier Soso is fictional.</Text></div><Card className="rounded-none p-7"><Heading level={2}>Product inquiry</Heading><div className="mt-6"><LeadForm fields={fields} submitLabel="Send product inquiry" /></div></Card></Container></Section></main>; }
