import { Card } from '@/components/capability/core/Card';
import { Container } from '@/components/capability/core/Container';
import { Heading } from '@/components/capability/core/Heading';
import { Section } from '@/components/capability/core/Section';
import { Text } from '@/components/capability/core/Text';
const projects = [['Harbour workspace', 'Commercial fit-out', 'Illustrative project-card layout for a flexible office environment.'], ['Cedar residence', 'Renovation', 'Illustrative project-card layout for a staged residential renovation.'], ['Market hall', 'Retail build', 'Illustrative project-card layout for a customer-facing commercial space.']];
export const metadata = { title: 'Projects — Forge Build', description: 'Illustrative construction project cards for the Forge Build capability demonstrator.' };
export default function ProjectsPage() { return <main><Section><Container width="wide"><p className="text-xs font-semibold uppercase tracking-[.18em]" style={{ color: 'var(--sc-primary)' }}>Projects</p><Heading level={1} className="mt-3">A portfolio that explains the work, not just the finish.</Heading><Text size="lg" tone="muted" className="mt-4 max-w-2xl">All projects below are fictional illustrative examples. They show the kind of information hierarchy a contractor portfolio can support; they make no completion, timeline, or performance claims.</Text><div className="mt-10 grid gap-6 md:grid-cols-3">{projects.map(([name, type, copy], index) => <Card key={name} className="rounded-none"><div className="flex aspect-[4/3] items-end border border-current/15 p-5" style={{ background: index === 1 ? 'var(--sc-accent)' : 'color-mix(in srgb, var(--sc-primary) 16%, transparent)' }}><span className="text-sm font-bold">Project / 0{index + 1}</span></div><p className="mt-6 text-xs font-semibold uppercase tracking-[.16em] opacity-60">{type}</p><Heading level={2} className="mt-3 text-2xl">{name}</Heading><Text tone="muted" className="mt-3">{copy}</Text></Card>)}</div></Container></Section></main>; }
