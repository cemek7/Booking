import { METHODOLOGY } from '@/showcase/content/methodology'; export const metadata={title:'Methodology — TechClave',description:'A clear, quality-gated delivery process.'}; export default function Methodology(){return <main className="mx-auto max-w-4xl px-6 py-20"><h1 className="text-5xl font-semibold">A delivery method built for clarity.</h1><ol className="mt-12 space-y-6">{METHODOLOGY.map((s,i)=><li key={s.name} className="border-l-2 border-slate-300 pl-6"><p className="text-sm text-slate-500">{String(i+1).padStart(2,'0')}</p><h2 className="text-2xl font-semibold">{s.name}</h2><p className="mt-2 text-slate-600">{s.objective}</p><p className="mt-2 text-sm"><b>Quality gate:</b> {s.qualityGate}</p></li>)}</ol></main>}
