import { CAPABILITIES } from '@/showcase/content/capabilities'; export const metadata={title:'Capabilities — TechClave',description:'Outcome-led web and product capabilities.'}; export default function Capabilities(){return <main className="mx-auto max-w-5xl px-6 py-20"><h1 className="text-5xl font-semibold">Capabilities organised by the outcome you need.</h1><div className="mt-12 grid gap-5 md:grid-cols-2">{CAPABILITIES.map(g=><section key={g.outcome} className="rounded-2xl border p-6"><h2 className="text-2xl font-semibold">{g.outcome}</h2><ul className="mt-4 list-disc pl-5 text-slate-600">{g.items.map(x=><li key={x}>{x}</li>)}</ul></section>)}</div></main>}
