import Link from 'next/link';
import { DEMONSTRATORS } from '@/showcase/content/demonstrators';
export const metadata = { title: 'Capability demonstrators', description: 'TechClave capability demonstrators by industry.' };
export default function WorkPage() { return <main className="mx-auto max-w-6xl px-6 py-20"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-slate-500">TechClave showcase</p><h1 className="mt-4 text-4xl font-semibold tracking-tight">Capability demonstrators, built one industry at a time.</h1><div className="mt-12 grid gap-5 md:grid-cols-2">{DEMONSTRATORS.map((demo) => <article key={demo.slug} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">{demo.projectType} · {demo.status === 'published' ? 'Published' : 'Planned'}</p><h2 className="mt-3 text-2xl font-semibold text-slate-950">{demo.name}</h2><p className="mt-2 text-sm text-slate-600">{demo.industry}</p><p className="mt-5 text-slate-700">{demo.oneLineSummary}</p>{demo.status === 'published' ? <Link className="mt-6 inline-block font-medium text-slate-950 underline underline-offset-4" href={`/showcase/demos/${demo.slug}`}>Explore demonstrator →</Link> : <p className="mt-6 text-sm font-medium text-slate-500">Coming soon</p>}</article>)}</div></main>; }
