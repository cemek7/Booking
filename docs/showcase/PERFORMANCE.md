# Performance review

No Lighthouse scores are reported: the local production build is blocked by the documented offline Google Fonts dependency, so measurements would not be valid. Showcase pages use static content and local assets where imagery is present. Run Lighthouse against a deployable build before claiming scores.
