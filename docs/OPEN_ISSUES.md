# Open issues

- `npm run build` remains environment-blocked by offline `next/font/google` fetching, as documented in the handover; this is not a showcase source regression.
- The pre-existing SunGrid `Heading id` type error and global Jest/Vitest assertion type noise remain out of scope for this branch.
- Capture responsive screenshots and browser Lighthouse results in an environment where the production build completes.
